#!/usr/bin/make -f
# -*- makefile -*-
export DESTDIR=$(CURDIR)/debian/tmp

%:
	dh $@ --with python2

override_dh_auto_install:
	$(MAKE) DESTDIR=$(DESTDIR) install-www
	$(MAKE) DESTDIR=$(DESTDIR) install-build
